import { createStyles } from '@mantine/core';

export const useStyles = createStyles({
  title: {
    textAlign: 'center',
    textTransform: 'uppercase',
    marginBottom: 16,
  },
  main: {
    marginTop: 32,
  },
  grid: {
    marginBottom: 16,
  },
  buttonWrapper: {
    textAlign: 'center',
  },
});
