import { createStyles } from '@mantine/core';

export const useStyles = createStyles({
  wrapper: {
    paddingTop: 32,
    paddingBottom: 32,
  },
  title: {
    textAlign: 'center',
    textTransform: 'uppercase',
    marginBottom: 16,
  },
  grid: {
    marginBottom: 16,
  },
  buttonWrapper: {
    textAlign: 'center',
  },
});
