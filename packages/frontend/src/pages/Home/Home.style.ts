import { createStyles } from '@mantine/core';

export const useStyles = createStyles({
  wrapper: {
    paddingTop: 16,
    paddingBottom: 16,
  },
  title: {
    textAlign: 'center',
    textTransform: 'uppercase',
    marginBottom: 16,
  },
  grid: {
    marginBottom: 16,
  },
  buttonWrapper: {
    textAlign: 'center',
  },
});
